import { css } from "styled-components";

const IntroTextStyle = css`
  font-size: 2.75rem;
  font-weight: 300;
  letter-spacing: -1px;

  b {
    font-weight: 600;
  }
`;

export default IntroTextStyle;
